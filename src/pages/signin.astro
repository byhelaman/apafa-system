---
import Layout from '../layouts/Layout.astro'
import AuthForm from '../components/AuthForm'

const { cookies, redirect } = Astro

const accessToken = cookies.get('sb-access-token')
const refreshToken = cookies.get('sb-refresh-token')

if (accessToken && refreshToken) {
  return redirect('/search')
}
---

<Layout title="Iniciar sesión | APAFA">
  <div class="h-full flex flex-col justify-center items-center -mt-32">
    <div class="w-full max-w-sm py-6">
      <div class="px-6 max-w-sm mb-6">
        <h1 class="text-3xl font-bold text-dark_blue">Hola!</h1>
        <p class="text-lg text-soft_blue">Te damos la bienvenida de nuevo</p>
      </div>
      <AuthForm client:load />
    </div>
  </div>
</Layout>
