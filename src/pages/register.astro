---
import Layout from "../layouts/Layout.astro";

import { RegisterForm } from "@/components/auth/RegisterForm";

import { Header } from '@/components/Header';

const { isloggin, role } = Astro.locals
---

<Layout title="Registrarse | APAFA">
  <section class="w-full max-w-lg px-6 mx-auto flex flex-col gap-16">
    <div class="my-5">
      <Header data={{isloggin, role}} client:load />
    </div>
    <div class="">
      <div class="mb-6">
        <h2 class="text-3xl font-bold mb-2">Registro en línea</h2>
      <p class="text-lg leading-6">Para los nuevos miembros de la APAFA</p>
      </div>
      <div class="pt-6 border-t-[1px] pb-10">
        <RegisterForm client:load/>
      </div>
    </div>
  </section>
</Layout>
